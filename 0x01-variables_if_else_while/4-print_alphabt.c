#include <stdio.h>
/**
 * main - Entry point
 *Return: 0(success)
 */
int main(void)
{
	int numb = 97;

	while (numb < 123)
	{
		if ((numb != 101) && (numb != 113))
		{
			putchar(numb);

		}

	}
	putchar('\n');
	return (0); }
